---
title: "Benchmark — <what against what>"
status: draft
owner: <name>
created: YYYY-MM-DD
updated: YYYY-MM-DD
milestone: <letter or task ID>
tags: [benchmark]
---

# Benchmark — <what against what>

<!--
Copy to research/benchmarks/YYYY-MM-DD-slug/README.md — a benchmark is a folder,
not a file, because it ships scripts and data alongside the prose.

  YYYY-MM-DD-slug/
    README.md      <- this file
    scripts/       <- everything needed to reproduce the numbers
    results/       <- raw output, committed
    figures/       <- charts generated from results/

A number that cannot be regenerated from scripts/ does not belong in this file.
Delete these comments once filled in.
-->

## Summary

<!-- The answer first: what won, by how much, and what the reader should do
about it. Two to four sentences. -->

## Question

<!-- The decision this benchmark is meant to inform, stated so it can be
answered. "Is the C++ core worth its integration cost for WAV parsing?" beats
"C++ vs Node performance". Name the milestone task it belongs to. -->

## What was measured

<!-- The operations timed, and why those and not others. Tie each to something
the product actually does. -->

## Equivalence check

<!-- The part people skip and reviewers ask about. Prove the implementations
being compared do the same work before comparing how fast they do it —
identical output on identical input, checked mechanically. Show the check and
its result. A benchmark of two different algorithms measures nothing. -->

## Method

- **Corpus:** <files, how they were generated or where they came from, sizes>
- **Repetitions:** <in-process reps × replicates, and which statistic is
  published — median resists outliers, mean does not>
- **Warm-up:** <what was discarded, and why: page cache, JIT tier-up>
- **Environment:** <CPU, RAM, OS, kernel, compiler and flags, runtime version,
  date>

<!-- State the environment even when it is unglamorous. A shared CI runner is a
valid place to measure, as long as the reader knows that is where the numbers
came from. -->

## Results

<!-- Tables generated from results/, not typed by hand. Include the run-to-run
spread so a reader knows which differences are real. -->

## Figures

<!-- Embed each as an image whose alt text states the finding, not the chart
type: "C++ decodes 2 to 12 times more samples per second than JavaScript",
not "bar chart". Source them from figures/, generated by a script in scripts/. -->

## Analysis

<!-- What the numbers mean. Where the two curves diverge and why. Any result
that surprised you — those are usually the useful ones. -->

## Threats to validity

<!-- What would change these numbers: a different machine, a different file
size, a warm vs cold cache, a bigger corpus. Be specific enough that a reader
can judge whether the conclusion survives their own conditions. -->

## Conclusion

<!-- The recommendation, and the threshold at which it would flip. -->

## Reproducing

```bash
# The exact commands, from a clean checkout.
```

## Related

- <the ADR this feeds, the issue it closes, prior benchmarks it extends>
